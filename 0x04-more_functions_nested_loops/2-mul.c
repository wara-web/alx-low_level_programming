#include "main.h"

/**
 * mul - multiplies two integers
 * @x: the first number to multiply
 * @y: the second number to mult the first number
 * Return: The result of multiplying the first to the second number
 */

int mul(int x, int y)
{
	return (x * y);
}
